import { IsZipValidDirective } from './is-zip-valid.directive';

describe('IsZipValidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsZipValidDirective();
    expect(directive).toBeTruthy();
  });
});
